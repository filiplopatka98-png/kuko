AuthType Basic
AuthName "KUKO Admin"
# IMPORTANT: replace with absolute path during deployment
# AuthUserFile /full/path/to/public/admin/.htpasswd
Require valid-user

RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
